[build]
  command = "npm run build"
  publish = "dist/integrity"

[build.environment]
  NODE_VERSION = "22"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[build.processing.html]
  pretty_urls = true
